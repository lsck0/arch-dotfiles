#!/usr/bin/env bash

source ~/.cache/wal/colors.sh

bemenu-run -p ">" \
    --cw 1 \
    --tb "$background" \
    --nb "$background" \
    --fb "$background" \
    --hb "$background" \
    --ab "$background" \
    --tf "$color1" \
    --hf "$color1"
