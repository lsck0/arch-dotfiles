[Service]
# Keep models resident so minuet's completion model isn't evicted by the embedding models cycling on the GPU, which turned every completion into a ~10s cold reload.
Environment=OLLAMA_KEEP_ALIVE=-1
