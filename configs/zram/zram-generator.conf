[zram0]
# Full-RAM sized compressed swap. zstd averages ~3:1, so even a full device
# only occupies roughly a third of actual RAM. A smaller fixed cap just means
# reclaim spills earlier onto disk swap (which we don't have) or the OOM killer.
zram-size = ram
compression-algorithm = zstd
swap-priority = 100
